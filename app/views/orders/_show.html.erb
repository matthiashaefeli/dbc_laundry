<h5> Status for order number <%= order.id %></h5>
<p>Order started on: <%= order.created_at.strftime("%B %d, %Y") %></p>
<p> Payed: <%= order.paid %>
  <% if order.paid == false %>
  <%= form_tag(new_charge_path, :method=>'get') do %>
  <%= hidden_field_tag "order_id", "#{order.id}"%>
  <%= submit_tag "pay now", class: "btn-link" %>
  <% end %>
  <% end %>
</p>
<p> Payment: <%= order.total %></p>

<div class="container">
  <div class="row">
    <div class="col">In box</div>
    <div class="col">Incomming</div>
    <div class="col">Processing</div>
    <div class="col"></div>
    <div class="col">Shipping</div>
    <div class="col">Delivered</div>
  </div>
  <div class="row">
  	<div class="col">
  		<div class="progress">
  	  <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100" style="width: <%= status_bar(order) %>%"></div>
  	</div>
  </div>
  </div>
</div>
<br>
<br>
